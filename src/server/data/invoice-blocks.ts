import type { EnrichedBlock } from "./block-hours-calculator";

export type { EnrichedBlock };

/** Backwards-compat alias — same shape as EnrichedBlock. */
export type InvoiceBlockWithHours = EnrichedBlock;
